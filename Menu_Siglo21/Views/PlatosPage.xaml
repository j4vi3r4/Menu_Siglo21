<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"             
             x:Class="Menu_Siglo21.Views.PlatosPage"
             BindingContext="{Binding Main, Source={d:StaticResource Locator}}"
             Title="Platos">
    <ContentPage.Content>
        <StackLayout
            BindingContext="{Binding Platos}">
            
            
            <CarouselView ItemsSource="{Binding Platos}">
                    <CarouselView.ItemTemplate>
                        <DataTemplate>
                        <StackLayout>
                            <Frame>
                                    <Grid BackgroundColor="Red" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" Padding="1">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Label FontAttributes="Bold" Grid.Row="0" Grid.Column="0" Text="{Binding Nombre}" HorizontalOptions="Start">
                                        </Label>
                                        <Label FontAttributes="Bold" Grid.Row="0" Grid.Column="1" HorizontalOptions="End" Text="{Binding Precio, StringFormat='{0}'}">
                                        </Label>
                                        <Label FontAttributes="Bold" Grid.Row="1" Grid.Column="0" Text="{Binding Disponibilidad}" HorizontalOptions="Start">
                                        </Label>
                                        <Label FontAttributes="Bold" Grid.Row="1" Grid.Column="1" HorizontalOptions="End" Text="{Binding Origen}">
                                        </Label>
                                    </Grid>
                            </Frame>
                        </StackLayout>
                    </DataTemplate>
                    </CarouselView.ItemTemplate>
                </CarouselView>            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>